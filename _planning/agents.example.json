{
  "agents": [
    {
      "name": "dataset_profile_agent",
      "description": "Profiles genomic datasets and emits shape, schema, and QC hints.",
      "command_template": "python -m orchestration.tasks.profile_dataset --input data/{dataset_file} --dataset {dataset_name} --output output/intermediate/{dataset_name}_profile.json",
      "cwd": ".",
      "env": {}
    },
    {
      "name": "hypothesis_scan_agent",
      "description": "Runs broad exploratory scans and candidate signal extraction.",
      "command_template": "python -m orchestration.tasks.hypothesis_scan --input data/{dataset_file} --dataset {dataset_name} --mode {mode} --output output/intermediate/{dataset_name}_scan.json",
      "cwd": ".",
      "env": {}
    },
    {
      "name": "validation_agent",
      "description": "Validates candidate findings against thresholds and writes final notes.",
      "command_template": "python -m orchestration.tasks.validate_findings --dataset {dataset_name} --input-a output/intermediate/{input_a} --input-b output/intermediate/{input_b} --min-score {min_score} --output output/intermediate/{dataset_name}_validated.json",
      "cwd": ".",
      "env": {}
    },
    {
      "name": "team_report_agent",
      "description": "Generates team-facing markdown and HTML reports in results.",
      "command_template": "python -m orchestration.tasks.report_team --validated-input output/intermediate/{validated_file} --report-base {report_base} --results-dir results",
      "cwd": ".",
      "env": {}
    }
  ]
}
